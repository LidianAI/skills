# Query

Search for endpoints:

```bash
lidian query --q "invoice OCR" --pageSize 1 --json
```

**Parameters:**
- `--q`: Search query
- `--pageSize`: Number of results (default: 1, max: 3)
- `--json`: Output as JSON

**Expected output:** Array of up to 3 endpoint results.
