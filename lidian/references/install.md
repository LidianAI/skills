# Install

Install the Lidian CLI for agent execution:

```bash
bunx @lidianai/cli --help
```

Or add it globally:

```bash
bun install -g @lidianai/cli
```

Verify installation:

```bash
lidian --help
```
